import 'package:flutter/cupertino.dart';

import '../utils/extensions.dart';

// examples of how to use the extension color
const white = '#FFFFFF';
Color w = HexColor.fromHex(white);

const black = '#000000';
Color b = HexColor.fromHex(black);
